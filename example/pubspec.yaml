name: flutter_network_core_example
description: Example app for flutter_network_core.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.8
  flutter: '>=3.24.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.8.0+1
  flutter_network_core:
    path: ../

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
