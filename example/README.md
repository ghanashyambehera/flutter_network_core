# flutter_network_core example

Demonstrates login, authenticated GET, 401 refresh, offline errors, and session expiry using an in-memory mock adapter (no live backend).

```bash
cd example
flutter run
```

Works on Android, iOS, Web, macOS, Windows, and Linux.
